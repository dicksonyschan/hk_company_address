# HTTP & Async
httpx[http2]>=0.27.0
aiolimiter>=1.1.0
tenacity>=8.3.0

# Data processing
polars>=0.20.0
duckdb>=0.10.0

# String / address processing
# rapidfuzz>=3.9.0  # P3 #23: 已移除，原始碼未使用；如需模糊比對再加回
# unicodedata2>=15.1.0  # P3 #23: 已移除，Python 內建 unicodedata 已足夠
opencc-python-reimplemented>=0.1.7

# Utilities
pyyaml>=6.0.1
tqdm>=4.66.0
chardet>=5.2.0
click>=8.1.7
pyarrow>=15.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

